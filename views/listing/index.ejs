<% layout("/layouts/boilerplate") %>

<!-- HERO SECTION -->
<section class="hero-carousel mb-5">
  <div id="heroCarousel" class="carousel slide carousel-fade" data-bs-ride="carousel">

    <div class="carousel-inner">
      <div class="carousel-item active">
        <div class="hero-slide" style="background-image:url('https://images.unsplash.com/photo-1505691938895-1758d7feb511')">
          <div class="hero-overlay">
            <h1>Find your next perfect stay</h1>
            <p>Homes • Experiences • Memories</p>
          </div>
        </div>
      </div>

      <div class="carousel-item">
        <div class="hero-slide" style="background-image:url('https://images.unsplash.com/photo-1522708323590-d24dbb6b0267')">
          <div class="hero-overlay">
            <h1>Live like a local</h1>
            <p>Handpicked stays across India</p>
          </div>
        </div>
      </div>

      <div class="carousel-item">
        <div class="hero-slide" style="background-image:url('https://images.unsplash.com/photo-1500530855697-b586d89ba3ee')">
          <div class="hero-overlay">
            <h1>Escape the ordinary</h1>
            <p>Mountains • Beaches • Farms</p>
          </div>
        </div>
      </div>
    </div>

  </div>
</section>


<!-- FILTER BAR (UNCHANGED LOGIC) -->
<div id="filters" class="filters-ui">
  <div class="filter" data-category="Trending">
    <i class="fa-solid fa-fire"></i><p>Trending</p>
  </div>
  <div class="filter" data-category="Rooms">
    <i class="fa-solid fa-bed"></i><p>Rooms</p>
  </div>
  <div class="filter" data-category="Ionic Cities">
    <i class="fa-solid fa-mountain-city"></i><p>Ionic Cities</p>
  </div>
  <div class="filter" data-category="Mountain">
    <i class="fa-solid fa-mountain"></i><p>Mountain</p>
  </div>
  <div class="filter" data-category="Castles">
    <i class="fa-brands fa-fort-awesome"></i><p>Castles</p>
  </div>
  <div class="filter" data-category="Pools">
    <i class="fa-solid fa-person-swimming"></i><p>Pools</p>
  </div>
  <div class="filter" data-category="Camping">
    <i class="fa-solid fa-campground"></i><p>Camping</p>
  </div>
  <div class="filter" data-category="Farms">
    <i class="fa-solid fa-cow"></i><p>Farms</p>
  </div>

  <div class="tax-toggle ms-auto">
    <div class="form-check-reverse form-switch">
      <input 
      class="form-check-input" 
      type="checkbox" 
      role="switch" 
      id="flexSwitchCheckDefault">
      <label class="form-check-label" for="flexSwitchCheckDefault">Display Taxes</label>
    </div>
  </div>
</div>

<% if (noResults) { %>
   <div class="alert alert-warning text-center">
       <%= noResults %>
   </div>
<% } %>

<% if (error.length > 0) { %>
   <div class="alert alert-danger"><%= error %></div>
<% } %>

<!-- LISTINGS -->
<div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1 mt-4">
  <% for(let listing of allListing){ %>
    <a href="/listings/<%= listing._id %>" class="listing-link">
      <div class="card col listing-card" data-category="<%= listing.category %>">
         
         <div class="badge-top"></div>
            <div class="wishlist">
            <i class="fa-regular fa-heart"></i>
         </div>

        <img src="<%= listing.image.url %>" class="card-img-top" style="height:20rem;">
        <div class="card-body">
          <p class="card-text">
            <b><%= listing.title %></b><br>
            ₹ <%= listing.price.toLocaleString("en-IN") %> / night
            <i class="tax-info"> + 18% GST</i>
          </p>
        </div>
      </div>
    </a>
  <% } %>
</div>

<script>
// for tax-toggle button
let taxSwitch = document.getElementById("flexSwitchCheckDefault");
   taxSwitch.addEventListener("click", ()=>{
   let taxInfo = document.getElementsByClassName("tax-info");
   for(info of taxInfo) {
      if(info.style.display != "inline"){
         info.style.display = "inline";
      }else{
         info.style.display = "none";
      }
   }
   });
</script>

<!--for category in index page-->
<script>
   document.addEventListener("DOMContentLoaded", function () {
    const filters = document.querySelectorAll(".filter");
    const listings = document.querySelectorAll(".listing-link");
    const flashMessage = document.getElementById("flash-message"); // Flash message container

    filters.forEach(filter => {
        filter.addEventListener("click", function () {
            let selectedCategory = this.getAttribute("data-category");
            let found = false; // Flag to track if a listing is found

            listings.forEach(listing => {
                let listingCategory = listing.querySelector(".listing-card").getAttribute("data-category");

                if (selectedCategory === listingCategory) {
                    listing.style.display = "block";
                    found = true; // At least one listing found
                } else {
                    listing.style.display = "none";
                }
            });

            // Show flash message if no listing found
            if (!found) {
                flashMessage.textContent = "No listing found in this category";
                flashMessage.style.display = "block";

                // Hide the message after 3 seconds
                setTimeout(() => {
                    flashMessage.style.display = "none";
                }, 3000);
            } else {
                flashMessage.style.display = "none"; // Hide message if listings exist
            }
        });
    });
});

</script>

<!-- TRUST SECTION -->
<section class="trust-section">
  <div class="trust-grid">
    <div>
      <i class="fa-solid fa-shield-halved"></i>
      <h5>Secure Payments</h5>
      <p>All payments are protected & encrypted.</p>
    </div>
    <div>
      <i class="fa-solid fa-location-dot"></i>
      <h5>Verified Locations</h5>
      <p>Only trusted & verified listings.</p>
    </div>
    <div>
      <i class="fa-solid fa-headset"></i>
      <h5>24/7 Support</h5>
      <p>We are here whenever you need us.</p>
    </div>
  </div>

</section>

<section class="featured-section mt-5">
  <h3 class="mb-4">Featured Destinations</h3>
  <div class="row g-4">
    <div class="col-md-4">
      <div class="feature-card">🏔️ Manali</div>
    </div>
    <div class="col-md-4">
      <div class="feature-card">🏖️ Goa</div>
    </div>
    <div class="col-md-4">
      <div class="feature-card">🌆 Jaipur</div>
    </div>
  </div>
</section>

<section class="stats-section mt-4 mb-4">
  <div class="stats-grid">
    <div><h2>10K+</h2><p>Happy Guests</p></div>
    <div><h2>2K+</h2><p>Verified Listings</p></div>
    <div><h2>100+</h2><p>Cities Covered</p></div>
  </div>
</section>

